import os
import shutil

class TextRenderer:
    """Pass-through rendering (Text rendering removed)."""
    
    def render_text(self, image_path, payload):
        """Returns the input image unchanged."""
        return image_path
